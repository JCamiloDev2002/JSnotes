// writting js code

/*
some important thing  to keep in mind

#FORMATTING CODE
code need to be formatted well, you have to stick a few basic formatting rules

## indentation and whitespaces
- usa saltos de linea al temrinar una instruccion
- usa una indentation para definir el bloque de codigo al que pertenece
*/

//without indentation and whitespaces
let status = "new"; let scared = "true"; if (satus === "new"){console.log("welcome to JS");}

//with whitespaces but whithout indentation
let satus2 = "new";
let scared2 = "true";
if (status2 === "new"){
console.log("welcome to JS");
}

//with whitespaces and indentation
let status3 = "new";
let scared3 = "true";
if(status3 === "new"){
  console.log("welcome to JS");
}

/*
 ## semicolons
 - despues de cada instruccion debe insertar un punto y coma( es solo para instrucciones separadas no para if o loops )
 
 ## code comments
 - no quieres ejecutar parte del codigo
 - agregar datos como autor, descripcion, etc...
 - comentar una parte especifica del codigo y explicar lo que hace, que sucede o porque es asi
  tipos de comentarios:
  - comentarios de una sola linea
  - comentarios multi-linea

 */
